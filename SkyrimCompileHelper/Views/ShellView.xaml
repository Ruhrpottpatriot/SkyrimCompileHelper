<Window x:Class="SkyrimCompileHelper.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:cal="http://www.caliburnproject.org"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:vm="clr-namespace:SkyrimCompileHelper.ViewModels"
        xmlns:views="clr-namespace:SkyrimCompileHelper.Views"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:ShellViewModel, IsDesignTimeCreatable=True}"
        cal:Bind.AtDesignTime="True"
        Title="MainWindow"
        SizeToContent="WidthAndHeight">
    
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal"
                    Grid.Row="0">
            <StackPanel  Margin="0,0,3,5">
                <Label Content="Skyrim Path:" />
                <TextBox x:Name="SkyrimPath">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="LostFocus">
                            <cal:ActionMessage MethodName="SaveSettings" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </TextBox>
            </StackPanel>

            <StackPanel Margin="3,0,0,5">
                <Label Content="Mod Organizer Path:" />
                <TextBox x:Name="OrganizerPath">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="LostFocus">
                            <cal:ActionMessage MethodName="SaveSettings" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </TextBox>
            </StackPanel>
        </StackPanel>

        <StackPanel Orientation="Horizontal"
                    Grid.Row="1"
                    Margin="0,5,0,5">
            <TextBlock Text="Repository:"
                       Margin="0,0,3,0" />
            <ComboBox cal:Message.Attach="AddRepository()"
                      ItemsSource="{Binding Repositories}"
                      SelectedItem="{Binding SelectedRepository}"
                      MinWidth="100"
                      Margin="3,0,0,0">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>

        <Grid Margin="0,5,0,0"
              Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal"
                        Margin="0,0,0,5"
                        Grid.Column="0"
                        Grid.Row="0">
                <TextBlock Text="Name:"
                           Margin="0,0,3,0" />
                <TextBox Text="{Binding SelectedRepository.Name}"
                         Margin="3,0,0,0"
                         MinWidth="75" />
            </StackPanel>

            <StackPanel Orientation="Horizontal"
                        Margin="5,0,0,0"
                        Grid.Row="0"
                        Grid.Column="1">

                <TextBlock Text="Version:"
                           Margin="0,0,3,0" />
                <TextBox Text="{Binding SelectedRepository.Version}"
                         Margin="3,0,0,0"
                         MinWidth="75">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MouseDoubleClick">
                            <cal:ActionMessage MethodName="ChangeVersion" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </TextBox>
            </StackPanel>

            <StackPanel Orientation="Horizontal"
                        Grid.Row="1"
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Margin="0,5,0,5">
                <TextBlock Text="Configuration:"
                           Margin="0,0,3,0" />
                <ComboBox ItemsSource="{Binding SelectedRepository.CompileConfigurations}"
                          SelectedItem="{Binding SelectedRepository.SelectedConfiguration}"
                          Margin="3,0,0,0"
                          MinWidth="100"
                          Height="22"
                          VerticalAlignment="Bottom">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>

            <StackPanel Grid.Row="2"
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Margin="0,5,0,5">
                <CheckBox x:Name="EnableCustomFlags"
                          Content="Enable Cumstom Flags" />
                <TextBox Text="{Binding SelectedRepository.SelectedConfiguration.CompilerFlags}"
                         IsEnabled="{Binding IsChecked, ElementName=EnableCustomFlags}" />
            </StackPanel>

            <Button Grid.Column="0"
                    Grid.Row="3"
                    Grid.ColumnSpan="2"
                    Margin="0,5,0,0"
                    x:Name="Compile"
                    Content="Compile" />
        </Grid>
    </Grid>
</Window>
